"""Everything the trained models do, and how sure they are.

Loading the artifacts, validating an input against the schema,
running the classifier and the regressor, explaining a prediction
with SHAP, wrapping it in a conformal interval, placing the user
in a persona, and comparing them with the training cohort.
"""
